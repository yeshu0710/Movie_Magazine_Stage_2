<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yeshu.moviemagazine">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/movie_magazine_ic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/movie_magazine_ic"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MovieInfo"
            android:parentActivityName=".Home"
            android:theme="@style/AppTheme1"></activity>

        <provider
            android:name=".MyContentProvider"
            android:authorities="com.example.yeshu.moviemagazine"
            android:enabled="true"
            android:exported="true"></provider>
    </application>

</manifest>